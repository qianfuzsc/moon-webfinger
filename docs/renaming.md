# Ownership and Publishing

The current module name is `qianfuzsc/moon-webfinger`, version `0.1.0`. The repository is <https://github.com/qianfuzsc/moon-webfinger>. The applicant and maintainer is 赵士超 (`qianfuzsc`).

The GitHub namespace and stable release version are selected. Before any future namespace or version change, update `moon.mod`, executable imports, CLI metadata, README status, and documentation examples, then rerun the complete verification matrix.
