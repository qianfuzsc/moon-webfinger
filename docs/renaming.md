# Renaming and Publishing

The current module name is `localdev/moon-webfinger`, version `0.1.0-dev`. It is not published, no repository is assigned, and the final maintainer namespace is not selected.

When an owner is selected, update the module name in `moon.mod`, root-package imports in executable `moon.pkg` files, CLI version metadata, README status, and documentation examples. Then rerun the complete verification matrix before creating any repository or publication. This local MVP does not perform those release actions.
