# Changelog

## 0.1.0-dev

- Added bounded RFC 7033 JRD parsing, deterministic serialization, request construction, querying, validation, response contexts, builders, auditing, and structured errors.
- Added a local-only nine-command CLI and five examples.
- Added deterministic cross-target tests, property cases, truncation coverage, documentation, and reproduction scripts.
